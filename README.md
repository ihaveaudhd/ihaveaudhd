# I Have AuDHD

A warm, accessible, neuroaffirming information site built with Next.js.

## Run locally

```bash
pnpm install
pnpm dev
```

Open `http://localhost:3000`.

## Deploy

Import this repository into Vercel. The framework will be detected automatically;
no custom build settings or environment variables are required for this first version.
